import React from 'react';
import styled from 'styled-components';

const ChatTopWrapper = styled.div`
  font-size: 24px;
  font-weight: 800;
`;

const ChatTop: React.FC = () => {
  return <ChatTopWrapper>CHAT</ChatTopWrapper>;
};

export default ChatTop;
